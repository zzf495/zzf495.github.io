source "https://rubygems.org"

gem "jekyll"

# Lean site features: social profiles and BibTeX-rendered publications.
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-scholar"
  gem "jekyll-sitemap"
  gem "jekyll-socials"
end

# al-folio v1.x runtime. Keep this list in sync with `_config.yml`.
group :al_folio_plugins do
  gem "al_folio_core", "= 1.0.15"
  gem "al_icons", "= 1.0.0"
  gem "al_folio_cv", "= 1.0.2"
  gem "al_citations", "= 1.0.1"
end
